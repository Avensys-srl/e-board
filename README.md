Technical constraints:
- Backend: PHP (procedural, no framework) compatible with PHP 7.2.11
- Environment: XAMPP
- Frontend: custom HTML/CSS (no Bootstrap)
- React: not used
- Database: MySQL

UI / Style:
- Custom HTML/CSS theme (warm palette, card-based layout, subtle motion)
- Consistent layout across auth, dashboard, and admin pages
- Optional CSS frameworks are acceptable if needed, but current UI uses custom CSS

Available pages:
- login.php
- register.php
- dashboard.php
- admin_users.php
